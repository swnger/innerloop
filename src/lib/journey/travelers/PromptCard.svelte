<div class="prompt-card" data-traveler="prompt-card" aria-label="Prompt: Fix the failing test">
	<span class="prompt-mark" aria-hidden="true">›</span>
	<span class="prompt-copy">Fix the failing test</span>
</div>

<style>
	.prompt-card {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		min-width: 12rem;
		padding: 0.7rem 0.85rem;
		border: 1px solid color-mix(in oklch, var(--concept-user) 42%, var(--c-line));
		border-radius: 0.72rem 0.72rem 0.72rem 0.18rem;
		background: var(--concept-user-fill);
		color: var(--concept-user);
		box-shadow: 0 0.45rem 1.1rem color-mix(in oklch, var(--concept-user) 14%, transparent);
		font: 600 0.82rem/1.25 var(--display);
		letter-spacing: -0.01em;
	}

	.prompt-mark {
		display: grid;
		place-items: center;
		width: 1.15rem;
		height: 1.15rem;
		border-radius: 999px;
		background: var(--concept-user);
		color: var(--concept-user-fill);
		font-size: 1rem;
		line-height: 1;
	}
</style>
